// ! Ternary Operator or Conditional Operator 

// ! Example
// * If a customer has more than 100 points
    // * they are a "GOLD" Customer
    // * Otherwise they are "Silver" Customer

// ? How to implement?
// ! Steps: 
// *1. First Declare a variable, to keep track of the number of points

let points = 110;
let type = points > 100 ? "Gold" : "Silver";
// * 1. start with a condition (points > 100)
// * 2. If that condition evaluation to true "Gold"
// * 3. If that condition evaluation to False "Silver"

console.log(type)

// if(points > 100) 
//     console.log('Gold')
// else
//     console.log('Silver')


// * Type represent type of customer
// * Here we use the ternary operator or conditional operator
