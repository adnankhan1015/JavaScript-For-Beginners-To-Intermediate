//* You might be dealing with a list of objects.
// The list of products in a shopping cart, or the list of colors the user has selected in situation like that, you use an array to store that list.

const person = {
    name: 'zohaib',
    age: 33,
}

// ! The type of the array is an object in JavaScript.

const selectedColors = ['red','green','blue',1 ,2, true, null, undefined];

selectedColors[8] = 'black'
console.log(selectedColors.length)

// * Array
// An array is a data structure, that we use to represent a list of items


